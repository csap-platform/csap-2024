#
# Setup
#

1. Project setup is java 17


- gradle repositories are not defined in the gradle build files, add them to ~/.gradle/init.gradle.kts
- alternately: cp docs/sample-init.gradle.kts ~/.gradle/init.gradle.kts

